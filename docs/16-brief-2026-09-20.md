# Brief 2026-09-20 — approved journey, fixes and release runbook

Baseline: `main` @ `e90ae3b` (16 Sep). Full suite green before and after: domain 21, concierge-core 265, vista-mock 84, concierge-api 100 (+1 offer-gate assertion), web 209, agent 199.

## What changed (3 patches, apply in order)

**Part 1 — API, tools, data**
- `prepare_payment` returns `needs: offer_decision` (+ offer card *Apply / Pay without it*) while an eligible saved-card offer is neither applied nor declined; new `offerDeclined` input. Root cause of the 18 Sep AED 56 miss.
- `prepare_swap` accepts `seats[]` (count validated; `SEATS_UNAVAILABLE` + `needs: swap_seats`). Widget seat map opened for an exchange sends "Use seats A4, A5 for the exchange" instead of a rejected `seat.select`.
- Session picker: no time / no history → anchor 19:00 (17:30 with children), late-night prints last, no family show after 21:30.
- Relaxed-date sentence ("No showtimes at MOE Monday…"); reporting always hides `diag_/audit_/validation-/hosted_qa_/qa_/test_/smoke_`; transfer counts labelled "handover requests · conversations".
- Bank-offer cancellation refusal → "can't be cancelled through self-service — Customer Care can review it"; `WKGRP33` archived at schedule refresh so Rahul has one live booking.
- New `resend_ticket` tool (email/SMS, ownership-verified, delivery log in conversation metadata, re-renders QR).

**Part 2 — widget (UI only)**
- Premium polish on the existing palette (header gradient + accent hairline, lit orb, glass chips, gradient CTAs and launcher halo, refined composer/hold bar, reduced-motion). Brand shown once (header). Copy: launcher "Make it a movie night", "Let's talk movies / I'd rather type", "Say it instead / Type it instead", "Go", placeholder "Movie night? Tell me what you're in the mood for…".
- Mute already mic-only in code (`micMuted` → input worklet / LiveKit mic track); confirm live.

**Part 3 — agent sources**
- `prompts/system.md`: new "How a great visit sounds" section carrying the brief's eight journeys and example lines (EN/AR).
- `procedures/booking.md`: `offer_decision` handling; `resend_ticket` use. `procedures/change-show.md`: seats into `prepare_swap`.
- `coaching/elevenlabs-simulations.json`: `resend_ticket` common mock.

## Release order
1. `git am 000{1,2,3}-*.patch` on `brief-2026-09-20`; PR → `main`; Railway deploys api/web/worker.
2. Recapture showtimes (horizon ends 23 Sep) → `SEED_FORCE=true` reseed → back to `false`.
3. Agent candidate: create branch `brief-2026-09-20` from `agtvrsn_6601m2m771maenbr550qmpgdp8vj`, then
   `pnpm --filter @voxi/agent agent:deploy --apply` with `ELEVENLABS_API_KEY`, `ELEVENLABS_AGENT_ID`, `ELEVENLABS_BRANCH_ID`, `ELEVENLABS_MAIN_BRANCH_ID=agtbrch_1701m1m6rk9vfpvr3dajwz6ak3eg`, `ELEVENLABS_EXPECTED_VERSION`, `ELEVENLABS_DEPLOYMENT_REPORT`, `CONCIERGE_PUBLIC_URL`, `TOOL_HMAC_SECRET`. Run the 98 attached tests + the scenario table on the candidate; promote to Main.
4. EU workspace → Developers → Webhooks: point endpoint `7bdf5dfc…` at `https://concierge-api-production-3d90.up.railway.app/webhooks/elevenlabs` with `ELEVENLABS_WEBHOOK_SECRET`; filter payloads by agent id.
5. Live check: scenario table (Sara ×4, James ×2, Rahul, refund method, failure→transfer) in EN + AR, text + voice; mute; Download QR.

## Open
- Repo is public; make private.
- Vista alignment P1s (refund keying, booking shape, order expiry, loyaltySessionToken) still with VOX IT for go-live.
