{
  "name": "voxi",
  "private": true,
  "version": "0.1.0",
  "description": "VOX 2.0 Digital Concierge (Voxi) \u2014 Phase 1 & 2. Conversational concierge on ElevenLabs Agents with a Vista-shaped backend.",
  "packageManager": "pnpm@10.28.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r --filter \"./packages/**\" --filter \"./apps/**\" build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm build:packages && pnpm -r --workspace-concurrency=1 test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "db:migrate": "pnpm --filter @voxi/db migrate",
    "db:seed": "pnpm --filter @voxi/db seed",
    "db:seed:history": "pnpm --filter @voxi/db seed:history",
    "dev:vista": "pnpm --filter @voxi/vista-mock dev",
    "dev:api": "pnpm --filter @voxi/concierge-api dev",
    "dev:worker": "pnpm --filter @voxi/worker dev",
    "dev:web": "pnpm --filter @voxi/web dev",
    "scrape": "pnpm --filter @voxi/scraper scrape",
    "build:packages": "pnpm -r --filter \"./packages/**\" build",
    "dev:packages": "pnpm -r --parallel --filter \"./packages/**\" exec tsc -p tsconfig.json --watch --preserveWatchOutput"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "typescript": "5.7.3",
    "vitest": "3.0.5",
    "tsx": "4.19.2",
    "@types/node": "22.13.1"
  }
}
